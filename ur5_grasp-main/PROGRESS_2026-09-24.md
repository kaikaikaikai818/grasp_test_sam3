# 项目进度记录（2026-09-24）

## 总目标

接收工具名称后，机器人完成：前往约 3 米外的工具区，D455 固定相机粗定位，
确认目标类别，D435i 手内相机精定位，安全抓取指定工具，并递回操作人员。
目标工具为活动扳手、螺丝刀、卷尺、钳子和胶带切割器。最终程序应一键运行，
测试菜单和逐步按键只保留给调试与维护。

## 已完成并经实机验证

- 相机分工固定：D455（`215122257404`）负责手外粗定位，D435i
  （`215222074676`）负责手内精定位。
- 复用现有核心标定，不按工具重新标定：`camera_pose.txt`、
  `cam2end_20260906.txt` 和 `camera_20260906.ini`。
- 双相机一次性一致性补偿已经独立验收：中位残差 6.35 mm，最大残差
  8.27 mm；本机结果保存在 `camera_alignment.json`，所有工具共用。
- 固定支撑面已保存，基座坐标高度约为 0.0214 m；运行时会先用实时深度确认，
  下降后继续使用下降前锁定值，避免夹爪遮挡造成平面跳变。
- 已完成 D455 粗定位、D435i 精定位、高位方向对齐、无接触下降、低力闭爪、
  抬升、松爪、姿态恢复和垂直安全回升的完整螺丝刀流程。
- 2026-09-24 实机完整抓取成功：下降前锁定平面 0.0214 m，低位复测差值
  0.4 mm，夹持前姿态误差 0.000°，闭爪前姿态误差 0.007°，夹爪确认
  `reached=1`，抓取后抬升 50 mm，松爪后恢复标准姿态并回升至 250 mm。
- 已修复支撑面单位重复换算和下降后固定平面丢失问题。下降前必须同时锁定
  固定支撑面、夹持 TCP 高度和净空，否则不允许运动。
- 当前螺丝刀抓取高度采用实时手柄厚度，并向下修正 3 mm；TCP 中心距离固定
  支撑面的硬下限为 8 mm。该 8 mm 是安全门槛，不是固定抓取高度。

## 当前适用范围

- 已确认一把螺丝刀可以完成完整抓取流程。
- 不同尺寸螺丝刀会重新估算手柄厚度；估算直径小于 10 mm 时主动拒绝。
- 3 mm 向下修正目前只属于螺丝刀策略，不直接用于活动扳手、钳子、卷尺或
  胶带切割器。
- 其他四类工具目前只能进入视觉和坐标验证，尚未授权实际下降和夹持。
- 视觉类别确认仍需继续现场验证；专用模型训练暂缓，先跑通全部流程。

## 下次开始时的第一项任务

验证一把更小的螺丝刀。真机先拉取提交 `8aa78d6`，运行
`screwdriver_workflow.bat` 的菜单 9，按 `P → Y → D`。确认画面中的夹持点位于
手柄中段，并核对终端输出的手柄厚度、`向下修正=-3.0mm`、夹持 TCP 和
`净空≥8.0mm`，再按 `R`。完成后记录实际夹持位置；若仍有系统性高度误差，
依据实测偏差只调整螺丝刀中心修正量，不再改变固定支撑面或双相机标定。

## 后续任务顺序

1. 用大小不同、方向不同的螺丝刀完成重复试验，冻结螺丝刀抓取参数和拒绝规则。
2. 将螺丝刀已验证的流程整理为统一工具接口，使主流程只负责相机切换、安全门控、
   运动、夹爪和恢复，各工具只提供抓取区域、高度、方向、力度和禁止区域。
3. 依次接入活动扳手、钳子、卷尺和胶带切割器；每类先做视觉与无接触验证，
   再开放低力抓取。
4. 完成多工具同场选择与错误类别拒绝，按真实现场数据决定是否训练专用分类模型。
5. 将测试按键流程收敛为一键主程序：文字指定工具 → D455 粗定位 → 类别确认 →
   D435i 精定位 → 抓取 → 递送；测试菜单继续独立保留。
6. 最后接入移动底座约 3 米往返、升降系统和向操作人员递送，并记录完整耗时与
   首抓成功率。

## 不需要重复的工作

更换工具、改变工具位置、改变提示词或更换同类工具尺寸，不需要重新生成
`camera_alignment.json`，也不需要重新标定固定支撑面。只有相机支架或末端相机
重装、TCP 改变、分辨率改变、核心标定文件改变，或双相机误差持续超限时，才重新
验证或标定。
